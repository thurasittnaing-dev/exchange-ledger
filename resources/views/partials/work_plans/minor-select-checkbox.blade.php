<input type="checkbox" class="form-check-input minor-plan-select" value="{{ $workPlan->id }}"
    data-month="{{ (int) $workPlan->plan_month }}" data-year="{{ (int) $workPlan->plan_year }}"
    aria-label="Select {{ $workPlan->case_no }}">
